<%= l(:text_audit_comment_added, :id => "##{@audit.id}", :auditor => h(@auditor)) %>
<hr/>
<%= render :partial => 'audit', :formats => [:html], :locals => { :audit => @audit, :audit_url => @audit_url, :changeset => @changeset, :repository => @repository } %>
