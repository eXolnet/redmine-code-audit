<%= raw @changesets.map {|changeset| {
      'id' => changeset.id,
      'label' => "#{changeset.revision}: #{truncate changeset.comments.to_s, :length => 60}",
      'value' => changeset.revision
      }
    }.to_json
%>
